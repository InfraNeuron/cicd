# cicd

CODE --> BUILD CODE--> TEST CODE--> APP -->IMAGE --> DEPLOY
GITHUB     MAVEN        MAVEN   SPRINBOOT  DOCKER    EC2

Docker is installed on common bastion, jenkins is running, docker ps -a , and start the container running at port 9090.
Jenkins admin user, both username pas pass is admin.
